{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "customer-service.fullname" . }}-postgres
  labels:
    {{- include "customer-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.postgres.password | quote }}
  database-url: {{ printf "postgresql://%s:%s@%s-postgres:%v/%s" .Values.postgres.username .Values.postgres.password (include "customer-service.fullname" .) .Values.postgres.port .Values.postgres.database | quote }}
{{- end }}
