Port-forward the API:
  kubectl port-forward service/{{ include "customer-service.fullname" . }}-app 8000:{{ .Values.service.port }} --namespace {{ .Release.Namespace }}

Port-forward the DB:
  kubectl port-forward service/{{ include "customer-service.fullname" . }}-postgres {{ .Values.postgres.port }}:{{ .Values.postgres.port }} --namespace {{ .Release.Namespace }}
Then open http://localhost:8000/docs.
{{- if .Values.web.enabled }}

Port-forward the onboarding web app:
  kubectl port-forward service/{{ include "customer-service.fullname" . }}-web {{ .Values.web.service.port }}:{{ .Values.web.service.port }} --namespace {{ .Release.Namespace }}
Then open http://localhost:{{ .Values.web.service.port }}.
Configured public URL for email links: {{ include "customer-service.webPublicBaseUrl" . }}
{{- end }}
